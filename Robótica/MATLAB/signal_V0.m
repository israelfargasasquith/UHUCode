function [salida] = signal_V0(tiempoVirtual)
%Primera funcion, fijarse en la sintaxis
%Matlab utiliza el nombre del fichero para llamarla en el programa, hay que
%fijarse en el nombre del fichero y de la funcion que estamos modificando.
%Los cálculos se hacen en radianes, pero nosotros los mostraremos en grados
salida = sin(tiempoVirtual);
end